var canvas = document.getElementById('canvas');
var divHeight = document.getElementById('game').clientHeight;
var divWidth = document.getElementById('game').clientWidth

canvas.height = divHeight;
canvas.width = divWidth;